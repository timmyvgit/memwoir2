const startTime = 10;
let timeRemain = startTime * 60;

const countDown = document.getElementById('countDown');

function updateCountDown() {
    const minutes = Math.floor(time / 60);
    let seconds = time % 60;

    countdownEl.innerHTML =tim--
}